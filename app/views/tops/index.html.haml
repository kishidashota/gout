= render "shared/header"
.toppage
  .toppage__content
    .toppage__content--title
      Gout
    .toppage__content--description
      居酒屋ごとのビールの銘柄を確認できます。
      自分のお気に入りの居酒屋(メニュー)を投稿することもできます。
    .buttons
      .link
        - if user_signed_in?
          = link_to posts_path do
            始める
        - else
          = link_to new_user_session_path do
            始める
      .link
        = link_to top_path(:id) do
          使い方
        
    